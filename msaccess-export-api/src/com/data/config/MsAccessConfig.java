package com.data.config;

import javax.sql.DataSource;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.jdbc.datasource.DriverManagerDataSource;

@Configuration
public class MsAccessConfig {
	
	/*
	 * @Bean(name = "dataSource") DataSource dataSource() { DriverManagerDataSource
	 * driverManagerDataSource = new DriverManagerDataSource();
	 * driverManagerDataSource.setUrl(
	 * "jdbc:ucanaccess://src/com/data/config/AGSFormFilling.accdb");
	 * driverManagerDataSource.setDriverClassName(
	 * "net.ucanaccess.jdbc.UcanaccessDriver"); return driverManagerDataSource; }
	 */

}
